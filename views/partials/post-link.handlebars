<div class="card">
  <div class="card-body">
    <a href="/posts/{{id}}">
      <h3>{{title}}</h3>
    </a>
    <p class="post-details">Posted by {{user.username}} on {{format_date createdAt}}</p>
  </div>
</div>